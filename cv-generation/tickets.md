# Jira Tickets

- When saving Experience or projects the in the input where the user adds the deatils of the experience (separated by enter) we are having a weird experience for users:
    - If users try to edit the item after saving, the formatting for that input is being lost so the information is broken and showing up separated by ','. Which causes the same behavior in the preview.
    - It will be nice if we can show a format with vinetas in the input as the user types items in the input field so is clear for the user that that is the correct way (with enters).
    - If we decide go keep this enter approach we need to add a way of letting the user know that is how it works. (Tooltip, Disclaimer or similar).

- We need to allow users to upload a photo, some templates display a photo

- The data that we render in the templates should be populated with Jhon Doe - Jane Doe data, so that users get to see the templates when they enter the editor.

- Friendly validation on the dates, so thaat the initial date cannot be greater than the finish date.
